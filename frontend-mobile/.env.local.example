# Server-side only. Never exposed to the browser: all traffic is proxied
# through Next.js route handlers so these hostnames stay inside the Docker network.
CATALOG_API_URL=http://stylistai-catalog:8002
ADK_API_URL=http://stylist-agent:8015
ADK_APP_NAME=stylist_agent
